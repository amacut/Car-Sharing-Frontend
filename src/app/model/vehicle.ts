export interface Vehicle {
  id: number;
  vehicleType: string;
  vehicleBrand: string;
  vehicleModel: string;
  drivingPrice: number;
  stopOverPrice: number;
  distancePrice: number;
  registration: string;
  currentFuel: number;
  currentRange: number;
  latitude: number;
  longitude: number;
}
